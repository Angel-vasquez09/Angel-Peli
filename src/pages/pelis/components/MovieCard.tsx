import React, { FC } from 'react'
import { Result } from '../../../interfaces'

interface IMovie {
    pelicula: Result;
}

export const MovieCard: FC<IMovie> = ({ pelicula }) => {
    return (
        <div>

        </div>
    )
}
